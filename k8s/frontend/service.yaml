apiVersion: v1
kind: Service
metadata:
  name: frontend-service
  namespace: overcookied
  labels:
    app: overcookied-frontend
spec:
  type: ClusterIP
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  selector:
    app: overcookied-frontend
